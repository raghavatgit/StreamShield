[package]
name = "shield_dll"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
name = "shield_dll"
crate-type = ["cdylib"]

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = [
  "winuser", "minwindef", "windef",
  "memoryapi", "handleapi"
] }
